import SwiftUI

extension Theme.Text {
    public struct Style {
        public let font: Font
        public let color: Color

        public init(font: Font, color: Color) {
            self.font = font
            self.color = color
        }
    }
}
